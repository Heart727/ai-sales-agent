# Python 虚拟环境（本地装依赖用的，不要上传，新机器自己重建）
.venv/
.worktrees/

# 环境变量文件（里面有 API Key，绝对不能上传）
.env

# 数据库文件（运行中自动生成，不用上传）
*.db

# Python 缓存文件（自动生成，没用）
__pycache__/
*.pyc

backups/
*.log
*.db-wal
*.db-shm
.vercel
.env*
